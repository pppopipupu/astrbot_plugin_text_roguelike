# 魔法肉鸽卡牌游戏插件开发规范文档 (AGENTS.md)

本文档面向在此项目上进行开发和维护的 AI Agent 及开发人员，定义了本项目的目录结构、代码开发规范以及测试准则。

## 1. 项目目录结构

整个插件的目录结构设计如下：

- `main.py`: 插件主要入口，用于注册 /rogue 命令，进行多级子指令分发并截获未处理异常（已兼容无 AstrBot 纯 Python 环境下直接导入）。
- `play_rogue.py`: 项目根目录下的控制台游戏入口，提供免前缀快捷命令行交互游玩。
- `metadata.yaml`: 插件元数据配置文件，声明插件版本、唯一标识等。
- `AGENTS.md`: 本项目规范文档（本文档）。
- `game/`: 游戏核心逻辑包。
  - `__init__.py`: 包初始化空文件。
  - `cards.py`: 静态卡牌配置桥接与数据导出。
  - `engine.py`: 顶层游戏引擎接口。
  - `event_impl.py`: 事件节点等交互逻辑的桥接。
  - `core/`: 核心引擎包。
    - `battle_engine.py`: 战斗推进逻辑、回合状态结算与节点转换。
    - `cli_router.py`: 用户命令行分发路由，实现指令的 ActionHandler 和 CommandHandler 分发。
    - `event_bus.py`: 事件总线订阅与广播机制。
    - `explore_engine.py`: 荒野探索、宝箱房、奇妙商店、篝火等节点逻辑。
    - `map_engine.py`: 关卡地图网络拓扑结构生成、节点移动和路由控制。
  - `data/`: 静态配置与文本模板数据字典。
    - `card_data.py`: 全体卡牌的基础映射字典与卡牌桥接属性配置。
    - `neutral_card_data.py`: 中立卡牌的属性、伤害/护盾/治疗参数与反馈模板数据。
    - `wizard_card_data.py`: 法师职业卡牌的属性、伤害/护盾/治疗参数与反馈模板数据。
    - `minion_data.py`: 我方全体随从的属性、技能参数与反馈模板。
    - `enemy_data.py`: 敌方全体角色意图、召唤物属性与意图说明数据。
    - `amulet_data.py`: 全体护符的默认耐久与结算效果参数。
    - `buff_data.py`: 全体战斗 Buff 的展示名称与效果描述配置。
    - `event_data.py`: 荒野事件的场景叙述、选项参数及选项触发反馈模板。
    - `relic_data.py`: 全体遗物的属性、稀有度、售价与效果描述配置。
  - `models/`: 数据模型及底层管理。
    - `state.py`: 玩家、敌人、随从、卡牌、护符等实体状态的 Dataclass 模型。
    - `events.py`: 包含 BattleStartEvent 等 14 个原子事件的定义。
    - `manager.py`: 本地存档序列化、载入及销毁管理。
  - `entities/`: 实体行为与逻辑多态实现。
    - `effects.py`: 原子游戏效果定义。
    - `cards/`: 卡牌逻辑包，包含 registry.py，base.py，neutral.py，wizard.py，legendary.py，curse.py 等卡牌具体打出效果类实现。
    - `buffs/`: 包含 buffs.py，继承自 BuffImpl 的各种战斗 Buff 响应逻辑。
    - `relics/`: 包含 relics.py，包含各种遗物的被动监听逻辑实现。
    - `minions/`: 随从技能子类多态逻辑。
    - `amulets/`: 护符特有钩子回调实现。
    - `enemies/`: 各种怪物与 Boss 意图的具体执行逻辑。
    - `events/`: 随机事件触发选项分支的具体回调逻辑。
  - `renderer/`: 游戏状态文本渲染层。
    - `__init__.py`: 代理至各渲染子模块的 GameRenderer。
    - `menu.py`: 菜单、卡牌库及玩家牌组渲染。
    - `battle.py`: 战斗界面及简要战斗状态渲染。
    - `map.py`: 地图路线预览及可达节点渲染。
    - `explore.py`: 荒野事件、奇妙商店、古老宝箱、战利品等界面渲染。
    - `query.py`: 随从、遗物、Buff及卡牌模糊匹配查询效果渲染。
- `scratch/`: 本地临时测试与仿真目录。
  - `test_game.py`: 包含交互式与自动随机仿真的控制台测试程序。
  - `test_flow.py`: 单元测试流程，用于验证第一层先古契约与商店购买、地图生成。
  - `test_echo_form.py`: 验证回响形态在不同层数下的触发频次以及多张卡牌依次触发的逻辑正确性。
  - `test_inner_shop.py`: 局内商店逻辑单元测试，模拟并验证地图中商店节点处商品展示、金币价格折扣、商品购买以及净化服务等功能正确性。
  - `test_awaiting_target_cancel.py`: 动作穿透及目标选择状态自动取消功能验证，测试在等待卡牌目标时输入新动作指令能否实现流畅的无缝切换。
  - `test_battle_logs_persistence.py`: 战斗日志持久化测试，确保战斗过程产生的临时消息不被保存至磁盘以避免下回合渲染时信息残留。
  - `test_new_cards_system.py`: 验证新卡牌效果、伤害计算系统和伤害播报格式的正确性。
  - `test_card_upgrade.py`: 验证卡牌升级强化系统与奥术熔炉事件功能正确性。

---

## 2. 代码规范

为确保多端运行顺畅与规范统一，在编写代码时必须严格遵守以下规范：

### 2.1 注释约束
- 绝对不要在 Python 源代码（包括 main.py, game/*.py, scratch/*.py）及 JSON 配置文件中编写任何注释。

### 2.2 视觉与渲染规范
- 游戏中的渲染逻辑全部集中于 game/renderer/ 模块包中。
- 游戏渲染给机器人的交互文本输出中，禁止使用任何 Markdown 标记（例如加粗 **, 斜体 *, 表格符 |, 代码块包围 ```）及 LaTeX 公式，防止在 QQ OneBot 平台无法正常显示。
- 游玩过程中可以使用 Emoji 和 Unicode 符号进行界面美化与排版对齐。
- 敌方在场上的意图展示被整合拼接为 (A: xx, BA: xx) 的分立格式。
- 引入树形地图列表预览机制，列表层级渲染玩家当前所处层及往后推 3 层的全部路径及可达节点去向，规避非等宽字体排版错乱。
- 玩家在进入战斗、事件、商店等界面时，顶部如果拥有遗物则会自动拼装显示为带有遗物图标的文本列表。

### 2.3 核心数据与状态规范
- 数据结构须继承 models/state.py 中的 Dataclass，状态更新均须通过 models/manager.py 中的 SaveManager 完成序列化存盘。
- 玩家初始手牌上限为 12。若拥有遗物【虚空面具】，则手牌上限将被强行限制为 9。
- 玩家每回合开始重置及战斗初始化时的基础动作扩容为 2A 1BA。
- 引入 0 动作消耗牌机制，当卡牌的 cost_a 与 cost_ba 为 0 时打出不扣除动作资源。
- 随从动作成长机制：随从在回合交替时 actions 和 bonus_actions += 累加，AA 每回合重置为 1。非 epic 随从无 BA，且技能提高为 2A 或 3A。
- 敌人行动与爪牙：普通/爪牙 1A 0BA，精英 1A 1BA，BOSS 远古红龙 1A 2BA，当存活敌人全为爪牙时自动判定玩家获胜。
- 引入眩晕（Stun）状态：处于该状态下的敌人在下一回合无法行动，并扣除一层眩晕 buff 恢复。
- Buff 逻辑多态与解耦规范：所有 Buff 的应用与修改逻辑从战斗引擎与卡牌类中抽离，统一实现于 `game/entities/buffs/buffs.py` 的 Buff 子类中。
- 回响形态触发机制：每回合打出的卡牌额外打出，每张牌最多回响 8 次，多余的层数会顺延回响每回合后续打出的卡牌。
- 护盾流失机制：在各自回合开始时，玩家和所有存活敌人的护盾均会自动失去一半（向下取整），并在回合更替的反馈信息中提供流失详情提示。
- 玩家手牌回合结束弃牌与保留机制：在玩家回合结束时，手牌中不具备保留（retain）效果的所有卡牌将被自动丢弃并移入弃牌堆。具有保留效果的卡牌继续保存在玩家手牌中。回合结束的丢弃操作绝不会触发卡牌的灵巧（agile）效果。
- 玩家战场随从格数量机制：玩家战场的随从和护符格子数量上限固定为 6 个，编号为 1 至 6，目标指向代码依次对应为 p1 至 p6。
- 卡组选择与移除序号映射机制：卡组列表展示时进行去重和字母升序排序渲染。在进行卡牌献祭（如古老宝箱房）或净化服务（商店移除卡牌）等需要通过序号移除卡组卡牌的场景时，系统内部将输入序号映射到经过排序去重后的卡组卡牌种类上，并对该种类的卡牌移除一张。
- 伤害类型系统与护盾穿透机制：引入了完整的 14 种伤害类型系统，真实（true）类型伤害能够无视并完全穿透护盾。
- 伤害播报格式：伤害播报统一为逗号分隔的扁平格式，即 `对【xxx】造成 xx 点 xx 伤害，对护盾造成 xx 伤害，对生命造成 xx 伤害`（仅当数值大于 0 时输出对应项），不使用括号，且如果生命/护盾某一个变动数值为 0 则不予播报该项；若变动数值均为 0（即未造成实际伤害），则播报尾部自动变更为 `（但xxx免疫了这次攻击！）` 的形式。

### 2.4 地图与契约机制
- 地图系统全长为 20 层，第 1 层固定为先古契约节点，第 5 和 15 层必定为古老宝箱房，第 10 和 20 层必定为首领战（第 10 层中首领，第 20 层最终首领），第 11 层为先古节点。
- 地图在进入第 2 层和第 12 层时分别一次性生成前半程或后半程的树形随机拓扑路径图并序列化存储。
- 开局先古契约提供 3 个随机选择项：界面石碑数量修改为三座，从“先古遗物”池和“先古卡牌”池中分别随机抽取 1 或 2 个选项（两者数量相加为 3 且随机选取，保证开局至少包含 1 个先古遗物和 1 个先古卡牌）。
- 第 11 层先古赐福是纯正面奖励，传奇卡牌池中加入流星爆与大法师的祈愿这两张传奇卡牌，并同样保持随机生成 3 个不重复的赐福包。
- 战斗与地图节点规范化：当在地图中选择 elite 或 boss 类型的节点去往战斗时，应当在 map_engine.py 内部统一将 run.node_type 规范化为 battle，以避免渲染阶段发生“未知关卡状态”的错误。
- 古老宝箱房要求玩家选择卡组中一张牌移除作为献祭，随后开启获得金币、随机珍奇卡牌与随机遗物奖励。
- 所有随机卡牌获取的场景（包括古老宝箱、篝火冥想、以及许愿泉投币事件）统一修改为提供三张备选卡牌以供三选一，且均支持跳过获取。

### 2.5 遗物（Relic）系统与商店联动
- 遗物分为普通、稀有、珍奇、诅咒四种稀有度，永久生效。
- 卡牌与遗物均具有“诅咒 (curse)”稀有度，用于标识所有的纯负面遗物（如铁锈之锁、愚者之誓、枯萎之种、盲目之障、税收契约）和诅咒卡牌（如晕眩、苦恼）。
- 遗物与卡牌的具体效果配置如下：
  - 【先古之眼】首回合多抽 2 卡；【准备背包】首回合多抽 1 卡且 +1BA；【重装甲片】/【坚固皮革】初始护盾 +8/+4；【铁锈之锁】/【贪婪契约】开局扣血 -4/-3；【能量核心】每回合 +1A；【红龙之血】胜利回血 5；【磨刀石】随从攻击 +1；【奥术符文】/【狂暴之印】法术伤害 +1/+2；【枯萎之种】禁止战斗治疗。
  - unstable_crystal (不稳定水晶): 玩家使用的法术伤害增加 1 点，且每回合开始时额外获得 1BA；但每当玩家使用法术牌时，玩家受到 1 点伤害。
  - vampiric_touch (吸血之触): 玩家打出伤害法术时回复 1 点生命值，但最大生命值上限永久减少 4 点。
  - ancient_page (先古残页): 战斗开始时，额外将 2 张【奥术星火】加入手牌，但战斗开始时玩家失去 4 点生命值。
  - blind_spot (盲目之障): 每场战斗第一回合开始时，玩家少抽 2 张牌。
  - tax_contract (税收契约): 每当进入下一个关卡时，玩家失去 6 金币。
  - meteor_swarm (流星爆): 消耗 2A 0BA，对所有敌人造成 8d12 随机火焰伤害。消耗。
  - archmage_wish (大法师的祈愿): 消耗 1A 1BA，获得 10 点护盾，本场战斗法术伤害 +4（祈愿奥术），并抽 2 张牌。消耗。
- 奇妙商店中会随机刷新出 1 个未拥有的遗物商品；若拥有【黄金罗盘】所有商品享受 6 折，拥有【贪婪契约】所有商品额外享受 6 折，两个折扣乘算叠加。

### 2.6 指令调度与队列系统
- 指令分支由 main.py 的统一分发器 _execute_sub_action 路由。所有指令以元组 (res_text, should_terminate) 形式返回。
- 随从指令支持 comma 逗号分隔的队列预解析，支持 all、* 等批量指挥随从指令。
- 回合结束指令会自动追加渲染战场最新画面和新抽取的手牌。
- 核心指令支持全量中英文变体解析：其中“牌组”新增英文别名 `deck`，“总览”新增别名 `overview`，“放弃”新增别名 `abandon`（同时放弃确认支持 `confirm` 参数）。
- 指令前缀配置项 `shortcut_prefix` 类型变更为 `list` 以支持多个前缀别名。匹配时对所有前缀按长度由长至短倒序排列，规避较短的前缀提前截断损坏指令单词。
- 引入用户独立的“免前缀肉鸽模式 (Rogue Mode)”：可以使用 `mode` 或 `模式` 指令控制开关。此状态持久化于用户的生涯数据 `stats_{user_id}.json` 中，默认对所有用户关闭。当某个用户开启后，其发送 of 无前缀消息若以有效游戏指令开头，或首个词为数字且该用户拥有活跃存档，则会被直接拦截匹配作为游戏指令执行，保障普通群聊闲聊消息放行不受误拦截。
- 提供“帮助”及“help”专用子命令，调用 GameRenderer.render_help() 列出全量指令的使用教程和中英文别名（包含免前缀模式提示），当输入未知指令时系统应当引导用户使用帮助指令。
- 随从攻击指令的自动目标选择：当发起随从攻击指令但未指定具体目标格子时，系统会自动将第一个存活的敌方单位作为攻击目标，保持与攻击型卡牌一致的行为。
- 局外商店购买子指令别名支持：在局外商店中，支持使用 `buy` 别名执行购买动作。
- 职业选择指令别名与快捷索引支持：在职业（class）管理中，支持使用 `c` 代替 `选择`，且选择时支持直接指定子职业序号（1、2）或 0（取消）。例如 `/rogue class c 1`、`/rogue class 1`、`/rogue class 0` 均被允许。

### 2.7 数据驱动与配置分离规范
- 静态配置数据化：所有卡牌、随从、怪物、护符、遗物、Buff、事件一律禁止硬编码，必须存放在 `game/data/` 的独立数据字典中。
- 反馈模板化：各实现类利用 `.format()` 动态渲染 `feedback` 模板并进行回填。

### 2.8 开闭原则 (OCP) 与事件驱动扩展规范
- 严格遵循开闭原则：对扩展开放，对修改封闭。
- 当引入新的遗物、Buff 或卡牌特性时，绝对禁止直接修改核心战斗引擎 (`battle_engine.py`) 或其它基础状态逻辑。
- 新增逻辑必须通过实现独立的监听器，并在加载时将其绑定到 `event_bus.py` 定义的原子事件（如 `BattleStartEvent`、`CardPlayedEvent`、`DamageCalculateEvent`）上来完成交互。确保任何特异性的判断和数值加成都内聚在具体实体类的回调中，不污染基础流转逻辑。

### 2.9 卡牌与指令模式重构规范
- 卡牌注册使用装饰器模式自动完成注册。全局统一在 `game/entities/cards/registry.py` 中导出 `@register_card(cid, **kwargs)`，在具体卡牌类声明处使用，并在 `base.py` 中通过 `inspect.signature` 获取参数自适应实例化，禁止任何硬编码的卡牌注册 `if-else` 分支。
- 命令行路由使用命令模式（Command Pattern）。全局将具体子动作和指令分别拆分为继承自 `ActionHandler` 和 `CommandHandler` 的命令类，在 `CLIRouter` 内部由映射字典分发，解耦并消除庞大的 `if-else` 结构，从而完全符合开闭原则。

---

## 3. 测试规范

本项目的验证应当严格按照自动化测试和边界测试进行：

### 3.1 本地测试手段
- 本地控制台交互式游玩：在项目根目录下执行：
  `py play_rogue.py`
  即可直接免前缀在命令行中游玩游戏。
- 本地交互式控制台测试：在插件根目录下执行：
  `py scratch/test_game.py`
  可在终端交互使用指令模拟游玩，验证指令容错性。
- 本地自动化仿真：在插件根目录下执行：
  `py scratch/test_game.py --auto`
  仿真程序会自动识别“进行中的游戏”提示并发送“开启 确认”强制覆盖重启，打印实时的命令交互日志，随机执行 150 步卡牌释放和行路动作。
- 本地单元流验证：在插件根目录下执行：
  `py scratch/test_flow.py`
  模拟并检验开局契约选卡、地图展示、遗物在商店刷出等主要步骤。
- 本地卡牌保留与回合丢弃验证：在插件根目录下执行：
  `py scratch/test_retain.py`
  验证回合结束时自动丢弃手牌、卡牌保留和灵巧卡牌绕过机制的正确性。
- 本地三选一卡牌及跳过验证：在插件根目录下执行：
  `py scratch/test_treasure.py`
  验证古老宝箱、篝火冥想与许愿泉三选一卡牌以及跳过获取的功能正确性。
- 本地免前缀模式与别名校验验证：在插件根目录下执行：
  `py scratch/test_rogue_mode.py`
  验证各用户开关状态隔离性、持久化存储落盘以及免前缀与指令别名拦截执行机制。
- 本地回响形态机制验证：在插件根目录下执行：
  `py scratch/test_echo_form.py`
  验证不同层数下，回响形态对于多张卡牌的触发限制与顺延效果。
- 本地局内商店功能验证：在插件根目录下执行：
  `py scratch/test_inner_shop.py`
  验证局内商店模块初始化、金币扣减、卡组变化、购买遗物以及净化服务与离开流程，同时检验相对导入模块正确性。
- 本地等待目标自动取消与新指令衔接验证：在插件根目录下执行：
  `py scratch/test_awaiting_target_cancel.py`
  验证当处于等待目标（awaiting_target）状态时，输入另一个合法的动作指令，能够自动取消旧目标选择并直接触发新动作的流程。
- 本地战斗日志持久化隔离验证：在插件根目录下执行：
  `py scratch/test_battle_logs_persistence.py`
  验证临时战斗日志战斗结束或新回合开始时不会残留到磁盘存档文件，确保下回合渲染时旧的消耗或洗牌日志不会被重复渲染打印。
- 本地新卡牌及伤害系统验证：在插件根目录下执行：
  `py scratch/test_new_cards_system.py`
  验证新中立卡牌、闪电链机制、真实伤害护盾穿透、死亡律动伤害类型以及全新伤害播报格式的正确性。
- 本地子职业别名与索引选择验证：在插件根目录下执行：
  `py scratch/test_class_command.py`
  验证子职业选择指令别名（c）与直接通过数字序号、0（取消）快速选择功能的正确性。
- 本地怪物意图同步与指针还原验证：在插件根目录下执行：
  `py scratch/test_enemy_action_sync.py`
  验证怪物被扣除动作点时其意图的即时同步取消、眩晕状态的即时渲染以及执行完成后临时指针的还原机制。
- 本地卡牌升级与熔炉事件验证：在插件根目录下执行：
  `py scratch/test_card_upgrade.py`
  验证卡牌升级机制、模糊和精确查询带+变体、营地商店强化服务以及炉温反噬与死亡律动等高级Buff的计算逻辑。

### 3.2 提测与通过指标
- 自动随机测试在 150 步内没有发生未捕获异常退出（即程序返回 exit code 0 且无 Python Traceback），方可判定为核心引擎通过测试。
- 修改 dataclass 时确保 models/manager.py 中的 to_dict 和 from_dict 序列化层同步更新，且向下兼容老版本存档。

### 3.3 测试豁免规则
- 针对仅涉及纯数值微调或修正的改动（例如修改卡牌的伤害、护盾、治疗数值等静态数据配置，而不涉及控制流、游戏状态逻辑修改或新功能开发），无需运行自动化仿真及流验证测试。
