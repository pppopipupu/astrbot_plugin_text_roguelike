version: '3.8'

services:
  astrbot-test-env:
    image: soulter/astrbot:latest
    container_name: astrbot-test-env
    restart: always
    ports:
      - "6186:6185"
    environment:
      - TZ=Asia/Shanghai
    volumes:
      - .:/AstrBot/data/plugins/astrbot_plugin_text_roguelike
